# Build Topology

The **Build Topology** operation allows you to create the topology from the mesh only model. Scoped entities  for topology creation can  be defined in **Topology Creation** control

The **Build Topology** operation has the following controls:

* **[Topology Creation](../controls/topology_creation.md)**: Defines the scope to create the topology from the mesh.
